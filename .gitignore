# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where third-party dependencies like ExDoc output generated docs.
/doc/

# Temporary files, for example, from tests.
/tmp/

# Generated browser assets.
/priv/static/assets/

# Local runtime logs.
/log/
/logs/

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Elixir language server and tooling.
/.elixir_ls/
/.fetch/

# Editor / OS temporary files.
.DS_Store
*.swp
*.swo
*~

# IDE folders.
.idea/
.vscode/
/bin/
/burrito_out/

# Local environment and auth artifacts.
.env
.env.*
.secrets
.credentials
status.txt
.claude/original-user-prompt.txt

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
cymphony_elixir-*.tar

# Per-user cymphony config (kept in ~/.cymphony/config.json, never in repo).
# The tracked WORKFLOW.md is a generic dev/test default — do NOT replace it
# with personal Linear/GitHub config, that lives in ~/.cymphony/config.json.

# Local design references (downloaded by the user as input to a redesign).
/design.md
/DESIGN.md
